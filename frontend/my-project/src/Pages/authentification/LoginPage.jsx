import React from 'react';
import Login from '../../components/views/authentification/Login';

export default function LoginPage() {
  // TODO: Add any specific role-based redirects here if needed
  // For login, usually no role check, but maybe a redirect if already authenticated
  return <Login />;
}
